import React  from "react";

const MatchingRequests = () => ( 

/*function matching requests when being a Plant Owner 
if (LocationSitter === Location Owner 
&& plantsOwner < plantsSitter 
&& beginningDateOwner >= beginningDateSitter
&& endDateOwner >= endDateSitter
) 
{
  return the name of the Plant Sitter available 
  and make it clickable
}

function matching requests when being a Plant Sitter 

if (LocationSitter === Location Owner 
&& plantsOwner < plantsSitter 
&& beginningDateOwner >= beginningDateSitter
&& endDateOwner >= endDateSitter
) 
{
  return the name of the Plant Sitter available 
  and make it clickable
}

*/

    <div>
        <label>
          This is your Dashboard
        </label>
    </div>
  )

export default MatchingRequests;